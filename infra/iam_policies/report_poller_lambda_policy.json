{
  "_comment": "lambda_handlers/report_poller.py execution role. Same secret-read scope as the requester (it independently refreshes/caches its own LWA access token per invocation) -- no S3 or Redshift access, since polling only reads report status.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "ReadOwnRefreshTokenSecrets",
      "Effect": "Allow",
      "Action": "secretsmanager:GetSecretValue",
      "Resource": "arn:aws:secretsmanager:${AwsRegion}:${AwsAccountId}:secret:ads-pipeline/profiles/*"
    },
    {
      "Sid": "WriteOwnLogs",
      "Effect": "Allow",
      "Action": ["logs:CreateLogGroup", "logs:CreateLogStream", "logs:PutLogEvents"],
      "Resource": "arn:aws:logs:${AwsRegion}:${AwsAccountId}:log-group:/aws/lambda/ads-report-poller:*"
    }
  ]
}
