{
  "_comment": "lambda_handlers/report_downloader.py execution role. Writes only to bronze/ (never silver/ or rejected/ -- those are the Glue validation job's job) and needs KMS encrypt permission for the bucket's SSE-KMS key (see infra/configure_bucket_security.py). No Secrets Manager access -- it receives the signed download URL from the poller's output, not from re-authenticating.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "WriteBronzeZoneOnly",
      "Effect": "Allow",
      "Action": "s3:PutObject",
      "Resource": "arn:aws:s3:::${RawBucketName}/bronze/*"
    },
    {
      "Sid": "EncryptWithBucketKmsKey",
      "Effect": "Allow",
      "Action": ["kms:GenerateDataKey", "kms:Decrypt"],
      "Resource": "${KmsKeyArn}"
    },
    {
      "Sid": "WriteOwnLogs",
      "Effect": "Allow",
      "Action": ["logs:CreateLogGroup", "logs:CreateLogStream", "logs:PutLogEvents"],
      "Resource": "arn:aws:logs:${AwsRegion}:${AwsAccountId}:log-group:/aws/lambda/ads-report-downloader:*"
    }
  ]
}
